{{#carousel-container}}
  {{#carousel-body}}
    {{#each model as |story|}}
      {{#carousel-item}}
      {{#link-to 'story' story.id}}
        <div class="caro-img">
          <img class=
        "img-responsive" src="{{story.image}}"/>
        </div>
      {{/link-to}}
      {{/carousel-item}}
    {{/each}}
  {{/carousel-body}}

  {{#carousel-arrow direction="left" class="carousel-control line-arrow left"}}
    <span class="glyphicon glyphicon-menu-left"></span>
  {{/carousel-arrow}}

  {{#carousel-arrow direction="right" class="carousel-control line-arrow right"}}
    <span class=
  "glyphicon glyphicon-menu-right"></span>
  {{/carousel-arrow}}
{{/carousel-container}}

<div class="stories-list">
  <ul>
    {{#each model as |story|}}
      {{story-tile story=story}}
    {{/each}}
  </ul>
</div>
